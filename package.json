{
  "name": "i-evolve",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "vitest run",
    "typecheck": "pnpm -r run typecheck"
  },
  "devDependencies": {
    "@types/node": "^20.17.0",
    "tsup": "^8.4.0",
    "tsx": "^4.22.4",
    "typescript": "^5.7.0",
    "vitest": "^3.1.0"
  }
}
